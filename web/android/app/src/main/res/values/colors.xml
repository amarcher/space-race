<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Space Race brand dark — the starfield boot color. Used for the Android 12+
         splash background and the launcher adaptive-icon background layer, so the
         launch is continuous with the in-app starfield (no white flash). -->
    <color name="spaceBg">#07071a</color>
</resources>
